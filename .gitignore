# 3rdparty/ holds upstream sources (BoofCV, BoofCPP, PyBoof) checked out for
# reading reference, not built into our deliverable. Keep them out of git for
# this repo — they are large and live on disk via separate clones.
3rdparty/

# Build outputs
build/
.gradle/
cmake-build-*/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
.cache/

# Per-image baseline dumps are regenerated by re-running the Java tool;
# only the aggregated tests/baseline.json is committed.
tests/regression/baseline_java/
tests/regression/baseline_cpp/
